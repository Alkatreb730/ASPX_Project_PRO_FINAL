


on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Test API Keys
        run: |
          echo "Testing OpenAI and CoinGecko Keys..."
          echo "OpenAI Key: ${{ secrets.OPENAI_API_KEY }}"
          echo "CoinGecko Key: ${{ secrets.COINGECKO_API_KEY }}"
